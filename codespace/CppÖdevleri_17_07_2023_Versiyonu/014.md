#### Sentaks hatası olan satırları belirtiniz:

```cpp

//auto a;SENTAKS HATASI: initialize edilmesi zorunlu
//int &b;SENTAKS HATASI: referanslaraın initialize edilmesi zorunlu
auto c = 10;
int &d = c;//Geçici nesne oluşturuluyor const olduğu için pr expression ile değer verilebilir.
const auto &e = 20;
int &f = ++c;
//int &g = c + 5;//SENTAKS HATASI: PR expression ile sol taraf referanslarına ilk değer verilemez.
int &&h = c % 2;
int func(); int &&j = func();
//int &foo(); int &&m = foo();SENTAKS HATASI: dönüş değeri int referans olduğu için. 
int ival = 10; int &&rval = ival + 10; int &p = rval;

```
